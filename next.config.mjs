/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images:{
        domains: ['scontent.cdninstagram.com','pbs.twimg.com'],
    }
};

export default nextConfig;
